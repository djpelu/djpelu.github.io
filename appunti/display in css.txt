display specifica se e quando un elemento è displayed


gli elementi hanno un display di default che è:
	- block-leveL: inizia in una nuova riga e occupa tutta la width -> h1, div, form, header, footer...
	- inline: non inizia alla nuova linea e non occupa tutta la linea -> span, img, a...

per gli elementi inline width e height non funzionano -> bisogna usare inline-block, che mantiene comportamento inline ma permette di mettere h e b



